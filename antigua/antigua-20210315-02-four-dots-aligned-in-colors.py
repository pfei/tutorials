
import turtle

alice = turtle.Turtle()

alice.up()
alice.dot(10, "blue")
alice.forward(50)
alice.dot(20, "red")
alice.forward(50)
alice.dot(30, "yellow")
alice.forward(50)
alice.dot(40, "green")

turtle.done()
